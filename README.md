# BED Assignment

## For Guest Feature:

- to know if user is a guest in the frontend: use
  `getIsGuest(token)` from helper.js
- to know if user is a guest from backend: use
  `const { isGuest } = req.user;`
  in the controller
